<nav class="white" role="navigation">
    <div class="nav-wrapper container">
        <a id="logo-container" href="{{ path('homepage') }}" class="brand-logo green-text">Envirias</a>
        <ul class="right hide-on-med-and-down ">
            {% if is_granted('ROLE_USER') == false %}
                <li><a class="green-text" href="{{ path('register') }}"><i class="material-icons left">input</i>Inscription</a></li>
                <li><a class="green-text" href="{{ path('login') }}"><i class="material-icons left">perm_identity</i>Connexion</a></li>
            {% else %}
                <li><a class="green-text" href="{{ path('myaccount') }}"><i class="material-icons left">perm_identity</i>{{ app.user.username }}</a></li>
                <li><a class="green-text" href="{{ path('logout') }}"><i class="material-icons left">link_off</i>Déconnexion</a></li>
            {% endif %}
        </ul>

        <ul id="nav-mobile" class="sidenav">
            {% if is_granted('ROLE_USER') == false %}
                <li><a class="green-text" href="{{ path('register') }}"><i class="material-icons left">input</i>Inscription</a></li>
                <li><a class="green-text" href="{{ path('login') }}"><i class="material-icons left">perm_identity</i>Connexion</a></li>
            {% else %}
                <li><a class="green-text" href="{{ path('myaccount') }}"><i class="material-icons left">perm_identity</i>{{ app.user.username }}</a></li>
                <li><a class="green-text" href="{{ path('logout') }}"><i class="material-icons left">link_off</i>Déconnexion</a></li>
            {% endif %}
        </ul>
        <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    </div>
</nav>
